class Main < Element =
  style = "cursor: pointer;"
  init_state = false
  text val = 
    if val
      return "this is ON"
    else
      return "off"
    end
  view a is_on =
    f := #{set_state !is_on}
    <div onclick={f}>
      {text is_on}
    </div>
