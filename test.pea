class Main < Element =
  style = "cursor: pointer;"
  init_state = false
  text val =
    if val
      "this is ON"
    else
      "off"
    end
  view _ is_on =
    <div onclick=#{set_state !is_on}>
      {text is_on}
    </div>
