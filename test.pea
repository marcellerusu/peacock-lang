class Main < Element =
  style _ is_on = "
    cursor: pointer;
    background: #{if is_on then "#f00" else "#fff" end};
  "
  init_state = false
  view _ is_on =
    <div onclick=#{set_state !is_on}>
      {{ [true]: "this is ON", [false]: "off like off" }[is_on]}
    </div>
