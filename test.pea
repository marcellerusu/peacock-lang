class Main < Element =
  style = "cursor: pointer;"
  init_state = false
  text val = 
    if val
      return "on"
    else
      return "off"
    end
  view a is_on =
    f := #{@set_state(true)}
    <div onclick={f}>
      {text is_on}
    </div>
