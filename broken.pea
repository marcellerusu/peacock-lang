# I believe it thinks
# that a in the body is a function
# like `a b = b`, but it doesn't output that
# so something more strange is going on
class Test =
  a = a
  b = b

# I would expect this to fail at run time
# or perhaps its only failing because
# non-paren calls can't be multi-line
# yes that would make sense
print(
  Test.new.get_arg 3,
  TestB.new.get_method
)